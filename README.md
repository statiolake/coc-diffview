# coc-diffview

Git diff and file-history views for coc.nvim. The file list and file history
are mounted as views in the `panel` supplied by `@statiolake/coc-ui`; selecting
a file opens a disposable two-pane Neovim diff backed by Git object contents.
